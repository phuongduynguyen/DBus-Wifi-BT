name: Nightly build

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

env:
  BUILD_TYPE: release

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Update dependencies
        run: sudo apt-get update && sudo apt install libdbus-1-dev

      - name: Building
        run: ./build.sh

